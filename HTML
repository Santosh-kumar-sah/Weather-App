<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Weather App</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="weather.css" />
</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-6">

        <div class="card shadow-lg border-0 rounded-4">
          <div class="card-body text-center p-4">

            <h1 class="mb-4 fw-bold text-primary">Weather App</h1>

            <div class="input-group mb-3">
              <input type="text" id="cityInput" class="form-control" placeholder="Enter city name">
              <button id="searchButton" class="btn btn-primary">Search</button>
            </div>

            <div id="errorMessage" class="alert alert-danger d-none" role="alert">
              City not found. Please try again.
            </div>

            <div id="weatherInfo" class="d-none">
              <img id="icon" class="mb-3" alt="Weather Icon" />
              <h2 id="cityName" class="fw-bold"></h2>
              <p id="temperature" class="display-6 fw-bold text-primary"></p>
              <p id="description" class="text-capitalize"></p>
              <p id="wind"></p>
              <p id="humidity"></p>
            </div>

          </div>
        </div>

      </div>
    </div>
  </div>

  <script src="weather.js"></script>
</body>
</html>
